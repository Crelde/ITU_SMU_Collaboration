namespace Server
{
    public enum UserType
    {
        admin,
        standard        
    };

    public enum RightType
    {
        view,
        edit
    };

    public enum FileType
    {
        other,
        text,
        image,
        audio,
        video        
    };
}
