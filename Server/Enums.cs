namespace Server
{
    public enum UserType
    {
        admin,
        standard        
    };

    public enum RightsType
    {
        view,
        edit
    };

    public enum FileType
    {
        other,
        text,
        image,
        audio,
        video        
    };
}
